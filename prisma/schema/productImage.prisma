model ProductImage {
    id        String @id @default(uuid())
    productId String
    url       String

    // Relations
    product Product @relation(fields: [productId], references: [id])

    @@index([productId])
    @@map("product_images")
}
